apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: portfolio-service-monitor
  namespace: portfolio
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: portfolio
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
